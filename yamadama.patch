diff --git a/static/openapi.yaml b/static/openapi.yaml
index c52503497c414cc694de3f3c08597eb847f17765..5577558f2c451627d1f140297d86a5efc6531fb1 100644
--- a/static/openapi.yaml
+++ b/static/openapi.yaml
@@ -8,118 +8,118 @@ info:
     Base URL:
     https://carriershark.com/api/v1
 
     Authentication:
     All endpoints require a company API key.
 
     Authorization header format:
 
     Authorization: Bearer cs_live_your_api_key
 
     Test key example:
     cs_test_123456789
   contact:
     name: CarrierShark Support
     email: support@carriershark.com
     url: https://carriershark.com
   license:
     name: Proprietary
 servers:
   - url: https://carriershark.com/api/v1
     description: Production v1 API
 security:
   - bearerAuth: []
 tags:
   - name: Carriers
-    description: Carrier lookup and company carrier management endpoints.
+    description: Search carriers and manage saved carriers.
   - name: Alerts
-    description: Alert retrieval and alert state mutation endpoints.
+    description: Retrieve alerts and manage alert status.
   - name: Contracts
-    description: Contract retrieval and contract state mutation endpoints.
+    description: Retrieve contracts and manage contract status.
   - name: Imports
-    description: Carrier import limit and bulk import endpoints.
+    description: Check import limits and import carriers in bulk.
 paths:
   /me/carriers/import-limits:
     get:
       tags: [Imports]
-      summary: Get import limits and current usage
+      summary: Get import limits
       description: |
-        Returns the authenticated company's carrier import plan limits and current saved-carrier usage.
-        Use this endpoint before bulk imports to understand total capacity and per-import caps.
+        Returns your plan's carrier import limits and current usage.
+        Use this before bulk imports to confirm total capacity and per-import caps.
       operationId: getCarrierImportLimits
       responses:
         '200':
           description: Import limits loaded successfully.
           headers:
             X-RateLimit-Limit:
               $ref: '#/components/headers/X-RateLimit-Limit'
             X-RateLimit-Remaining:
               $ref: '#/components/headers/X-RateLimit-Remaining'
             X-RateLimit-Reset:
               $ref: '#/components/headers/X-RateLimit-Reset'
           content:
             application/json:
               schema:
                 type: object
                 required: [plan, max_total, max_per_import, chunk_size, current_total]
                 properties:
                   plan:
                     type: string
                   max_total:
                     type: integer
                   max_per_import:
                     type: integer
                   chunk_size:
                     type: integer
                   current_total:
                     type: integer
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to load import limits.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to load import limits
 
   /me/carriers/import:
     post:
       tags: [Imports]
-      summary: Bulk import carriers into the authenticated company account
+      summary: Import carriers
       description: |
-        Imports multiple carrier DOT numbers into the authenticated company's saved carrier list.
-        The API normalizes DOT values, enforces per-import and plan-level limits, and reports invalid or skipped entries.
+        Imports multiple carrier DOT numbers into your saved carriers.
+        DOT values are normalized, limits are enforced, and invalid or skipped entries are returned.
       operationId: importCarriers
       requestBody:
         required: true
         content:
           application/json:
             schema:
               type: object
               required: [dots]
               properties:
                 dots:
                   type: array
                   description: Array of DOT numbers to import.
                   items:
                     type: string
             examples:
               bulkImport:
                 value:
                   dots: ["336075", "123456"]
       responses:
         '200':
           description: Import was processed.
           headers:
             X-RateLimit-Limit:
               $ref: '#/components/headers/X-RateLimit-Limit'
             X-RateLimit-Remaining:
@@ -161,54 +161,54 @@ paths:
                     type: array
                     items:
                       type: string
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to import carriers.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to import carriers
 
   /carriers:
     get:
       tags: [Carriers]
-      summary: Search carriers
+      summary: Search carrier database
       description: |
-        Search the carrier database using DOT, MC, legal name, DBA name, or location filters.
-        At least one search parameter must be provided.
+        Search carriers by DOT, MC, legal name, DBA name, or location.
+        At least one filter is required.
       operationId: searchCarriers
       parameters:
         - $ref: '#/components/parameters/DotQuery'
         - name: mc
           in: query
           schema:
             type: string
         - name: legalname
           in: query
           schema:
             type: string
         - name: dbaname
           in: query
           schema:
             type: string
         - name: city
           in: query
           schema:
             type: string
         - name: state
           in: query
           schema:
             type: string
         - $ref: '#/components/parameters/PageQuery'
         - $ref: '#/components/parameters/PageSizeQuery'
@@ -254,54 +254,54 @@ paths:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: At least one search parameter is required (dot, mc, legalname, dbaname, city, state)
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Search failed.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Search failed
 
   /carriers/{dot}:
     get:
       tags: [Carriers]
-      summary: Get a carrier by DOT number
+      summary: Get carrier
       description: |
         Returns a single carrier record for the provided USDOT number.
-        The response includes carrier table fields plus a derived `cargo_carried` array when cargo data exists.
+        Includes standard carrier fields plus `cargo_carried` when available.
       operationId: getCarrierByDot
       parameters:
         - $ref: '#/components/parameters/DotPath'
       responses:
         '200':
           description: Carrier record with cargo information.
           headers:
             X-RateLimit-Limit:
               $ref: '#/components/headers/X-RateLimit-Limit'
             X-RateLimit-Remaining:
               $ref: '#/components/headers/X-RateLimit-Remaining'
             X-RateLimit-Reset:
               $ref: '#/components/headers/X-RateLimit-Reset'
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/CarrierDetail'
               example:
                 dot: "336075"
                 dotnumber: "336075"
                 legalname: ACME TRANSPORT LLC
                 dbaname: ACME LOGISTICS
                 mc_number: "MC-654321"
                 phystreet: 123 MAIN ST
                 phycity: DALLAS
@@ -330,54 +330,54 @@ paths:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Carrier not found
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Database query failed.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Database query failed
 
   /me/carriers:
     get:
       tags: [Carriers]
-      summary: List carriers saved by the authenticated company
+      summary: List saved carriers
       description: |
-        Lists the carriers already saved for the authenticated company.
-        Supports the same filtering and pagination pattern as the public carrier search endpoint.
+        Returns the carriers saved to your account.
+        Supports the same filters and pagination as carrier search.
       operationId: listMyCarriers
       parameters:
         - $ref: '#/components/parameters/DotQuery'
         - name: mc
           in: query
           schema:
             type: string
         - name: legalname
           in: query
           schema:
             type: string
         - name: dbaname
           in: query
           schema:
             type: string
         - name: city
           in: query
           schema:
             type: string
         - name: state
           in: query
           schema:
             type: string
         - $ref: '#/components/parameters/PageQuery'
         - $ref: '#/components/parameters/PageSizeQuery'
@@ -416,54 +416,54 @@ paths:
                     last_polled_at: '2026-03-30T18:42:31.000Z'
                     added_at: '2026-03-01T14:25:00Z'
                 total: 1
                 page: 1
                 pageSize: 25
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to load company carriers.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to load company carriers
     post:
       tags: [Carriers]
-      summary: Save one or more carriers for the authenticated company
+      summary: Save carriers
       description: |
-        Saves one or more carrier DOT numbers for the authenticated company.
-        DOT values are normalized, checked against the carriers table, and reported as inserted, already saved, or invalid.
+        Saves one or more carrier DOT numbers to your account.
+        DOT values are normalized and reported as inserted, already saved, or invalid.
       operationId: addMyCarriers
       requestBody:
         required: true
         content:
           application/json:
             schema:
               type: object
               required: [dot]
               properties:
                 dot:
                   oneOf:
                     - type: string
                     - type: array
                       items:
                         type: string
             examples:
               single:
                 value:
                   dot: "336075"
               multiple:
                 value:
                   dot: ["336075", "123456"]
       responses:
         '200':
           description: Carrier save attempt results.
@@ -489,54 +489,54 @@ paths:
                   value:
                     error: dot is required
                 noValidDots:
                   value:
                     error: No valid DOT numbers provided
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to add carriers.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to add carriers
     delete:
       tags: [Carriers]
-      summary: Remove one or more saved carriers for the authenticated company
+      summary: Remove saved carriers
       description: |
-        Removes one or more saved carriers from the authenticated company's list.
-        Each submitted DOT is reported individually as deleted or not found.
+        Removes one or more saved carriers from your account.
+        Each submitted DOT is reported as deleted or not found.
       operationId: deleteMyCarriers
       requestBody:
         required: true
         content:
           application/json:
             schema:
               type: object
               required: [dot]
               properties:
                 dot:
                   oneOf:
                     - type: string
                     - type: array
                       items:
                         type: string
             examples:
               single:
                 value:
                   dot: "336075"
               multiple:
                 value:
                   dot: ["336075", "123456"]
       responses:
         '200':
           description: Carrier delete attempt results.
@@ -564,54 +564,54 @@ paths:
                 noValidDots:
                   value:
                     error: No valid DOT numbers provided
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to remove carriers.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to remove carriers
 
   /carriers/{dot}/alerts:
     get:
       tags: [Alerts]
-      summary: List alerts for a specific carrier
+      summary: List carrier alerts
       description: |
-        Returns API-channel alerts for a specific carrier DOT belonging to the authenticated company.
-        Error-state alerts are excluded, and results can be filtered by alert ID and status.
+        Returns alerts for a tracked carrier.
+        Filter by alert ID or status. Error-state alerts are excluded.
       operationId: listCarrierAlerts
       parameters:
         - $ref: '#/components/parameters/DotPath'
         - name: id
           in: query
           schema:
             type: string
           description: Alert identifier.
         - name: status
           in: query
           schema:
             type: string
           description: Alert status filter.
         - $ref: '#/components/parameters/PageQuery'
         - $ref: '#/components/parameters/PageSizeQuery'
       responses:
         '200':
           description: Alert list for the carrier.
           headers:
             X-RateLimit-Limit:
               $ref: '#/components/headers/X-RateLimit-Limit'
             X-RateLimit-Remaining:
               $ref: '#/components/headers/X-RateLimit-Remaining'
             X-RateLimit-Reset:
               $ref: '#/components/headers/X-RateLimit-Reset'
@@ -621,52 +621,52 @@ paths:
                 $ref: '#/components/schemas/AlertListResponse'
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to load carrier alerts.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to load carrier alerts
 
   /alerts:
     get:
       tags: [Alerts]
       summary: List alerts
       description: |
-        Returns API-channel alerts for the authenticated company across all tracked carriers.
-        Error-state alerts are excluded, and results can be filtered by alert ID, status, and DOT number.
+        Returns alerts across all tracked carriers in your account.
+        Filter by alert ID, status, or DOT number. Error-state alerts are excluded.
       operationId: listAlerts
       parameters:
         - name: id
           in: query
           schema:
             type: string
         - name: status
           in: query
           schema:
             type: string
         - name: dotnumber
           in: query
           schema:
             type: string
         - $ref: '#/components/parameters/PageQuery'
         - $ref: '#/components/parameters/PageSizeQuery'
       responses:
         '200':
           description: Alert list.
           headers:
             X-RateLimit-Limit:
               $ref: '#/components/headers/X-RateLimit-Limit'
             X-RateLimit-Remaining:
               $ref: '#/components/headers/X-RateLimit-Remaining'
             X-RateLimit-Reset:
@@ -695,106 +695,106 @@ paths:
                 total: 1
                 page: 1
                 pageSize: 25
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to load alerts.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to load alerts
 
   /alerts/new:
     get:
       tags: [Alerts]
-      summary: List new alerts only
+      summary: List new alerts
       description: |
-        Returns only alerts in `NEW` status for the authenticated company.
+        Returns alerts in `NEW` status only.
         Use this endpoint to poll for unprocessed alert activity.
       operationId: listNewAlerts
       parameters:
         - name: id
           in: query
           schema:
             type: string
         - name: dotnumber
           in: query
           schema:
             type: string
         - $ref: '#/components/parameters/PageQuery'
         - $ref: '#/components/parameters/PageSizeQuery'
       responses:
         '200':
           description: New alert list.
           headers:
             X-RateLimit-Limit:
               $ref: '#/components/headers/X-RateLimit-Limit'
             X-RateLimit-Remaining:
               $ref: '#/components/headers/X-RateLimit-Remaining'
             X-RateLimit-Reset:
               $ref: '#/components/headers/X-RateLimit-Reset'
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/AlertListResponse'
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to load NEW alerts.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to load NEW alerts
 
   /alerts/processed:
     patch:
       tags: [Alerts]
-      summary: Mark alerts as processed
+      summary: Mark alerts processed
       description: |
-        Marks one or more API-channel alerts as `PROCESSED` for the authenticated company.
-        Only matching alert IDs owned by the company are updated.
+        Marks one or more alerts as `PROCESSED`.
+        Only matching alert IDs in your account are updated.
       operationId: markAlertsProcessed
       requestBody:
         required: true
         content:
           application/json:
             schema:
               type: object
               required: [alerts]
               properties:
                 alerts:
                   type: array
                   items:
                     type: string
                   description: Numeric alert IDs.
             example:
               alerts: ["46", "47"]
       responses:
         '200':
           description: Alert update results.
           headers:
             X-RateLimit-Limit:
               $ref: '#/components/headers/X-RateLimit-Limit'
             X-RateLimit-Remaining:
               $ref: '#/components/headers/X-RateLimit-Remaining'
             X-RateLimit-Reset:
@@ -811,54 +811,54 @@ paths:
                 $ref: '#/components/schemas/Error'
               example:
                 error: alerts array is required (numeric ids)
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to mark alerts processed.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to mark alerts processed
 
   /alerts/unprocessed:
     patch:
       tags: [Alerts]
-      summary: Mark alerts as unprocessed
+      summary: Mark alerts new
       description: |
-        Reverts one or more API-channel alerts back to `NEW` status for the authenticated company.
-        Only matching alert IDs owned by the company are updated.
+        Marks one or more alerts back to `NEW`.
+        Only matching alert IDs in your account are updated.
       operationId: markAlertsUnprocessed
       requestBody:
         required: true
         content:
           application/json:
             schema:
               type: object
               required: [alerts]
               properties:
                 alerts:
                   type: array
                   items:
                     type: string
                   description: Numeric alert IDs.
             example:
               alerts: ["46", "47"]
       responses:
         '200':
           description: Alert update results.
           headers:
             X-RateLimit-Limit:
               $ref: '#/components/headers/X-RateLimit-Limit'
             X-RateLimit-Remaining:
               $ref: '#/components/headers/X-RateLimit-Remaining'
             X-RateLimit-Reset:
@@ -877,52 +877,52 @@ paths:
                 error: alerts array is required (numeric ids)
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to mark alerts unprocessed.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to mark alerts unprocessed
 
   /contracts:
     get:
       tags: [Contracts]
       summary: List contracts
       description: |
-        Returns contracts for the authenticated company with optional filters for status, DOT number, contract ID, and creation timestamps.
-        Results are ordered by newest created contract first.
+        Returns contracts in your account.
+        Filter by status, DOT number, contract ID, or created timestamp.
       operationId: listContracts
       parameters:
         - name: status
           in: query
           schema:
             type: string
         - name: dotnumber
           in: query
           schema:
             type: string
         - name: contract_id
           in: query
           schema:
             type: string
           description: UUID or numeric contract identifier.
         - name: created_after
           in: query
           schema:
             type: string
             format: date-time
         - name: created_before
           in: query
           schema:
             type: string
             format: date-time
@@ -961,98 +961,98 @@ paths:
                 total: 1
                 page: 1
                 pageSize: 25
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to load contracts.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to load contracts
 
   /contracts/new:
     get:
       tags: [Contracts]
-      summary: List completed contracts that are ready for broker processing
+      summary: List ready contracts
       description: |
-        Returns contracts in `COMPLETED` status for the authenticated company.
-        This route is intended for downstream systems that need contracts ready for broker-side processing.
+        Returns contracts in `COMPLETED` status.
+        Use this for contracts ready for broker-side processing.
       operationId: listNewContracts
       parameters:
         - $ref: '#/components/parameters/PageQuery'
         - $ref: '#/components/parameters/PageSizeQuery'
       responses:
         '200':
           description: Completed contracts.
           headers:
             X-RateLimit-Limit:
               $ref: '#/components/headers/X-RateLimit-Limit'
             X-RateLimit-Remaining:
               $ref: '#/components/headers/X-RateLimit-Remaining'
             X-RateLimit-Reset:
               $ref: '#/components/headers/X-RateLimit-Reset'
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/ContractListResponse'
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to load new contracts.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to load new contracts
 
   /contracts/send:
     post:
       tags: [Contracts]
-      summary: Create sent contracts for one or more carriers
+      summary: Send contracts
       description: |
-        Creates new contracts in `SENT` status for one or more carrier DOT numbers.
-        Submitted DOT values are normalized, validated against the carriers table, and invalid values are returned separately.
+        Creates contracts in `SENT` status for one or more carriers.
+        Submitted DOT values are normalized and invalid values are returned separately.
       operationId: sendContracts
       requestBody:
         required: true
         content:
           application/json:
             schema:
               type: object
               required: [dot]
               properties:
                 dot:
                   oneOf:
                     - type: string
                     - type: array
                       items:
                         type: string
                   description: Numeric DOT value or array of DOT values.
             examples:
               single:
                 value:
                   dot: "336075"
               multiple:
                 value:
                   dot: ["336075", "123456"]
       responses:
         '200':
@@ -1128,54 +1128,54 @@ paths:
                   value:
                     error: No valid DOTs found in carriers table
                     invalidDots: ["999999"]
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to send contracts.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to send contracts
 
   /contracts/processed:
     patch:
       tags: [Contracts]
-      summary: Mark completed contracts as processed
+      summary: Mark contracts processed
       description: |
-        Marks one or more `COMPLETED` contracts as `PROCESSED` for the authenticated company.
-        Contract IDs that do not belong to the company or are not currently completed are reported as not updated.
+        Marks one or more `COMPLETED` contracts as `PROCESSED`.
+        IDs not found in your account or not currently completed are reported as not updated.
       operationId: markContractsProcessed
       requestBody:
         required: true
         content:
           application/json:
             schema:
               type: object
               required: [contracts]
               properties:
                 contracts:
                   type: array
                   items:
                     type: string
                   description: UUID or numeric contract IDs.
             example:
               contracts: ["12", "13"]
       responses:
         '200':
           description: Contract update results.
           headers:
             X-RateLimit-Limit:
               $ref: '#/components/headers/X-RateLimit-Limit'
             X-RateLimit-Remaining:
               $ref: '#/components/headers/X-RateLimit-Remaining'
             X-RateLimit-Reset:
@@ -1192,54 +1192,54 @@ paths:
                 $ref: '#/components/schemas/Error'
               example:
                 error: contracts array is required (UUID or numeric ids)
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to mark contracts processed.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to mark contracts processed
 
   /contracts/unprocessed:
     patch:
       tags: [Contracts]
-      summary: Revert processed contracts back to completed
+      summary: Mark contracts completed
       description: |
-        Reverts one or more `PROCESSED` contracts back to `COMPLETED` for the authenticated company.
-        Contract IDs that do not belong to the company or are not currently processed are reported as not updated.
+        Marks one or more `PROCESSED` contracts back to `COMPLETED`.
+        IDs not found in your account or not currently processed are reported as not updated.
       operationId: markContractsUnprocessed
       requestBody:
         required: true
         content:
           application/json:
             schema:
               type: object
               required: [contracts]
               properties:
                 contracts:
                   type: array
                   items:
                     type: string
                   description: UUID or numeric contract IDs.
             example:
               contracts: ["12", "13"]
       responses:
         '200':
           description: Contract update results.
           headers:
             X-RateLimit-Limit:
               $ref: '#/components/headers/X-RateLimit-Limit'
             X-RateLimit-Remaining:
               $ref: '#/components/headers/X-RateLimit-Remaining'
             X-RateLimit-Reset:
@@ -1256,54 +1256,54 @@ paths:
                 $ref: '#/components/schemas/Error'
               example:
                 error: contracts array is required (UUID or numeric ids)
         '401':
           $ref: '#/components/responses/UnauthorizedError'
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to mark contracts unprocessed.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to mark contracts unprocessed
 
   /contracts/{contract_id}:
     get:
       tags: [Contracts]
-      summary: Get a contract by ID
+      summary: Get contract
       description: |
-        Returns a single contract and its associated carrier record for the authenticated company.
-        The `contract_id` path parameter accepts either a numeric identifier or a UUID.
+        Returns a single contract and its associated carrier record.
+        `contract_id` accepts either a numeric identifier or a UUID.
       operationId: getContractById
       parameters:
         - name: contract_id
           in: path
           required: true
           description: UUID or numeric contract identifier.
           schema:
             type: string
       responses:
         '200':
           description: Contract found.
           headers:
             X-RateLimit-Limit:
               $ref: '#/components/headers/X-RateLimit-Limit'
             X-RateLimit-Remaining:
               $ref: '#/components/headers/X-RateLimit-Remaining'
             X-RateLimit-Reset:
               $ref: '#/components/headers/X-RateLimit-Reset'
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/ContractEnvelope'
         '400':
           description: Invalid contract ID format.
           content:
@@ -1320,53 +1320,53 @@ paths:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Contract not found
         '429':
           description: Rate limit exceeded
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Rate limit exceeded
         '500':
           description: Failed to load contract.
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/Error'
               example:
                 error: Failed to load contract
 
   /carriers/{dot}/contracts:
     get:
       tags: [Contracts]
-      summary: List contracts for a carrier
+      summary: List carrier contracts
       description: |
-        Lists contracts for a specific carrier DOT within the authenticated company's account.
+        Lists contracts for a specific carrier in your account.
         The DOT path parameter must contain digits only.
       operationId: listCarrierContracts
       parameters:
         - $ref: '#/components/parameters/DotPath'
         - $ref: '#/components/parameters/PageQuery'
         - $ref: '#/components/parameters/PageSizeQuery'
       responses:
         '200':
           description: Contracts for the carrier.
           headers:
             X-RateLimit-Limit:
               $ref: '#/components/headers/X-RateLimit-Limit'
             X-RateLimit-Remaining:
               $ref: '#/components/headers/X-RateLimit-Remaining'
             X-RateLimit-Reset:
               $ref: '#/components/headers/X-RateLimit-Reset'
           content:
             application/json:
               schema:
                 $ref: '#/components/schemas/ContractListResponse'
         '400':
           description: Invalid DOT format.
           content:
             application/json:
               schema:
